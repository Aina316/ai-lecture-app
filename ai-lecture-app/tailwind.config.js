/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./index.html", // Vite's entry HTML file
    "./src/**/*.{js,ts,jsx,tsx}", // Scan all JS/TS files in the src directory
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}